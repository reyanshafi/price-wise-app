/**
 * Myntra scraper — deferred to SerpAPI Google Shopping.
 * Myntra is a fashion-only platform protected by Cloudflare + DataDome.
 * SerpAPI handles Myntra results as part of Google Shopping.
 */
export async function scrapeMyntra(query) {
  console.log("ℹ️ Myntra: using SerpAPI Google Shopping (see serpApiScraper.js)");
  return [];
}
